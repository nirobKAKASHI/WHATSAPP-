name: 🟣 Glitch Deployment (Manual Sync)

on:
  workflow_dispatch:

jobs:
  notify:
    runs-on: ubuntu-latest

    steps:
      - name: 📦 Repo Prepared for Glitch
        run: |
          echo "🛠️ This project is ready for Glitch!"
          echo "🔁 To deploy, go to https://glitch.com, create a new project, and import this repo."
          echo "✅ Make sure keepalive.js and package.json are set."
